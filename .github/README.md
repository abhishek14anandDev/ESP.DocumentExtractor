This repository uses GitHub Actions for Azure Function deployment.

Required repository secret:

- `AZURE_FUNCTIONAPP_PUBLISH_PROFILE_ESPDATAEXTRACTOR`

Create that secret by copying the full contents of the Azure Function App publish profile.
